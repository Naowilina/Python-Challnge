#Data from excel
import os
import csv

#set path for file
csvpath = os.path.join(".../class_activities/PYTHON/Starter_code/Pybank/resources/budget_data.csv")

#declare vales
total_month = []
total_profits = []
profit_changes = 0
monthy_chnages = []
previous_value = 0

# Open the CSV File
with open(csvpath) as csvfile:
    csvreader =csv.reader(csvfile, deliter=",")
    #Had to skip the header to or otherwise will count it as another month
    csv_reader=next(csvreader)

    for row in csvreader:

# Total of months & profits/loses
total_profits=[int(x) for x in total_profits] total_profits_sum=sum(total_profits) print (total_profits_sum) for i in range(len(total_profits)-1): monthly_changes.append
